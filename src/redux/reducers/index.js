const initialState = {
  search: {
    ricerca: {},
  },
  handleSection: {
    selezionato: [],
  },
};

const mainReducer = function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
};

export default mainReducer;
